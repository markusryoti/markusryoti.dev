---
const { className } = Astro.props;
---

<button
	class=`bg-accent text-brandwhite rounded-3xl w-40 h-12 font-bold uppercase ${className}`
>
	<div class="flex justify-center items-center h-full w-full">
		<slot />
	</div>
</button>
